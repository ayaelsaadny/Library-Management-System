@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
  @using book.ViewModel;
    @model BuyVM

<h2>Buy Book</h2>

@if (TempData["Message"] != null)
{
    <div class="alert alert-success">@TempData["Message"]</div>
    <a href="@Url.Action("AllBooks")" class="btn btn-secondary">Back to Books</a>
}
else if (TempData["Error"] != null)
{
    <div class="alert alert-danger">@TempData["Error"]</div>
    <a href="@Url.Action("AllBooks")" class="btn btn-secondary">Back to Books</a>
}

<div>
    <p><strong>Book Name:</strong> @Model.BookName</p>
    <p><strong>Author:</strong> @Model.Author</p>
    <p><strong>Status:</strong> @(Model.Availability ? "Available" : "Not Available")</p>
</div>

@if (Model.Availability)
{
    <form asp-action="BuyConfirm" method="post">
        <input type="hidden" name="BookId" value="@Model.BookId" />
        <button type="submit" class="btn btn-primary">Confirm Purchase</button>
        <a href="@Url.Action("AllBooks")" class="btn btn-secondary">Back to Books List</a>
    </form>
}
else
{
    <p class="text-danger">Sorry, this book is currently not available for purchase.</p>
}
